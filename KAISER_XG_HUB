local Rayfield = loadstring(game:HttpGet("https://sirius.menu/gen2"))()

local window = Rayfield:CreateWindow({
    name = "Example Hub",
    subtitle = "Rayfield Gen2",
})

local tab = window:CreateTab({ name = "Home", icon = 93364949241311 })

tab:CreateButton({
    name = "Say hello",
    callback = function()
        window:Notify({ title = "Hello", content = "Your first element works." })
    end,
})

tab:CreateToggle({
    name = "Auto Sprint",
    callback = function(value)
        print("Auto Sprint:", value)
    end,
})

-- 1. Auto-Execution Actions (Instant Chat Notification & Brookhaven RP Name/Bio Update)
local function onExecution()
    -- Send Instant Chat Message
    pcall(function()
        if TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            local generalChannel = TextChatService.TextChannels:FindFirstChild("RBXGeneral")
            if generalChannel then
                generalChannel:SendAsync("@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ 🔱⚔️KAISER_V3 SPAMMER LOADED⚔️🔱")
            end
        else
            ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer("@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@ 🔱⚔️KAISER_V3 SPAMMER LOADED⚔️🔱", "All")
        end
    end)

-- Example: Sending formatted RP name text safely
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Define formatted text strings
local customName = "THE eternity | Oc3 v3"
local customBio = "[ SPAM LORD ]"

-- Example function to fire an RP update (structure varies by game)
local function updateRPName(name, bio)
    local combinedDisplay = name .. " | " .. bio
    
    -- Check if the game uses custom attributes for display names
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        -- Games often update overhead GUIs or attributes via RemoteEvents
        -- Example: ReplicatedStorage.UpdateRPNameEvent:FireServer(combinedDisplay)
        print("Updated RP Display to:", combinedDisplay)
    end
end

-- Call the function
updateRPName(customName, customBio)
